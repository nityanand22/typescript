// Types Inferencing
// when typescript infers a type from value, it is called type inferencing e.g. let x = 10; x is of type number

// Type Annotation
// when we declare the type of a variable explicitly , it is called type annotation e.g. let x: number = 10;

let drink = "Cola";

let num = Math.random() > 0.5 ? 10 : "ten";

let drink2: string = "Cola";
// drink2 = 10; // error
